apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fleet
  labels:
    app: aether-fleet
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: aether-fleet
